# ── Web framework ────────────────────────────────────────────────────
fastapi
uvicorn[standard]==0.29.0
starlette
python-multipart

# ── Database ─────────────────────────────────────────────────────────
sqlalchemy
# SQLite async driver (local development)
aiosqlite
# PostgreSQL async driver (production — Neon / Supabase / Render PG)
asyncpg

# ── Config / env ─────────────────────────────────────────────────────
pydantic
pydantic-settings
python-dotenv

# ── LLM ──────────────────────────────────────────────────────────────
groq

# ── Embeddings (disabled on free tier by default — uses >512 MB RAM) ─
faiss-cpu
sentence-transformers

# ── Web scraping ─────────────────────────────────────────────────────
httpx
beautifulsoup4
playwright

# ── PDF generation (HTML → PDF) ──────────────────────────────────────
# WeasyPrint requires system libs; see render.yaml buildCommand
weasyprint
# Pure-Python fallback — zero system deps, always works
fpdf2

# ── PDF reading ───────────────────────────────────────────────────────
pypdf

# ── Email delivery ────────────────────────────────────────────────────
resend

# ── Scheduling ────────────────────────────────────────────────────────
apscheduler
pytz

# ── Utilities ────────────────────────────────────────────────────────
pandas
rapidfuzz
tenacity
loguru
